  int MissingNumber(vector<int>& array, int n) {
        // Your code goes here
        int sum=(n*(n+1))/2;
        int currsum=0;
        for(int i=0;i<n-1;i++){
            currsum=currsum+array[i];
        }
        int num=sum-currsum;
        return num;
    }
